services:
  - type: web
    plan: starter
    name: realworld
    runtime: image
    image:
      url: ghcr.io/vincentserpoul/realworld-go-oapi-codegen-chi-pgx:v0.0.12
    healthCheckPath: /sys/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: elephant
          property: connectionString
      - key: ENV
        value: prod
databases:
  - name: renderworld
