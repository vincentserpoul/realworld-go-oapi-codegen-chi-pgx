# this config is for your local environment
with_debug_profiler = true
env = "local"
database_url = "127.0.0.1"

secrets_path = "config/adminapi/local.secrets.toml" # pragma: allowlist secret

[log]
is_pretty = true
level = "debug"

[observability.collector]
host = "127.0.0.1"
